# Handoff — what's left on Raker

Submission deadline: **September 22, 2026, 12:00 PM PT**, via vibeapps.dev. Requires a public GitHub repo and a live URL (`convex.site` or `chatgpt.site`) — no localhost, no private repos.

## Blocking, do these first
1. **Get Raker its own AgentMail API key** (not the personal one from the earlier detour). Sign up / grab a key at agentmail.to, then:
   ```bash
   cd raker
   npx convex env set AGENTMAIL_API_KEY <key>
   ```
2. **Link to a real Convex account.** Right now this is a local, unauthenticated deployment (`anonymous-raker` on `127.0.0.1`) — Firecrawl's webhook can't reach it and it can't be deployed publicly.
   ```bash
   npx convex login
   npx convex dev   # re-run once linked, to push to your real dev deployment
   ```
3. **Create a public GitHub repo and push.** Currently only a local git repo with no remote configured.
   ```bash
   gh repo create raker --public --source=. --push
   ```

## Core feature work
4. **Seed a real crawl source.** The `sources` table is empty. Find an actual settlement-tracker site (e.g. a public class-action listings page) and add it:
   ```ts
   await ctx.runMutation(api.crawl.addSource, { name: "...", url: "https://..." });
   ```
5. **Run a real crawl and tune the extraction prompt.** [convex/crawl.ts](convex/crawl.ts)'s JSON-extraction prompt is a first guess — it hasn't been tested against real settlement-tracker HTML. Trigger `api.crawl.crawlSource` (use `mode: "poll"` locally — Firecrawl's webhook can't reach `localhost`) and iterate until it reliably extracts title/provider/category/eligibility/deadline/contact email.
6. **Register the AgentMail webhook** once you have a non-local deployment: `https://<your-deployment>.convex.site/agentmail/webhook`, with the matching `AGENTMAIL_WEBHOOK_SECRET` set via `npx convex env set`.
7. **Test the full claim-filing loop end to end**: provision the inbox from the dashboard → file a claim → send a real reply to the claim thread → confirm the dashboard flips the claim's status live. The reply-matching logic in [convex/email.ts](convex/email.ts) (matches by `threadId`, falling back to "oldest unanswered claim on this inbox") is untested against a live reply and may need refinement once you see real thread behavior.
8. **Decide on auth.** None is implemented; it's optional per the rules, but worth a conscious decision either way rather than a default.

## Polish
9. **Frontend.** [src/App.tsx](src/App.tsx) is a bare-bones single page (inline styles, no loading/error states). Worth a design pass before recording the demo.
10. **Add an actual `eslint.config.*`.** `package.json` has a `lint` script but no config file exists yet.
11. **Decide what to do with the archived hack-new scaffold** at `../settlement-hunter-hacknew-archive/` — delete it once you're sure nothing in its theme/bounty-doc setup is worth pulling in.
12. There's an uncommitted diff in `convex/_generated/api.d.ts` (auto-regenerated by `convex dev` after some scratch-file churn) — harmless, just needs a commit or a `git checkout` to discard, your call.

## Deploy
13. ```bash
    npm run build
    npx convex deploy
    npx @convex-dev/static-hosting deploy --dist dist
    ```
    Live URL will be `https://<your-deployment>.convex.site`.

## Submission requirements (don't skip)
14. Run `/hackathon` (the installed `convex-hackathon-skill`) to generate/update `hackathon.md` — it's never been run yet, so that file doesn't exist.
15. Record a demo video, **under 3 minutes**, showing the real product (not just talking).
16. Post the build on X or LinkedIn, tagging **@convex, @openai, @firecrawl, @agentmail**. Required by the rules, and engagement counts toward scoring.
17. Submit at the exact link before the deadline: https://vibeapps.dev/judging/convex-all-gas-hackathon-openai/submit
