# Demo path

1. **Raker finds class-action settlements you qualify for, files the claim by email, and tracks the reply live.** — go to `/`
2. **This is the live dashboard, backed by Convex with settlements discovered by a Firecrawl crawl.** — (narration only)
3. **Provisioning Raker's dedicated AgentMail claims inbox.** — click `text="Provision claims inbox"`
4. **Each row is a real open settlement. One click files the claim, and the administrator's reply flips its status live. No polling, no forms.** — (narration only)

Everything not listed above is stubbed, hardcoded, or deleted. That is the correct prioritisation under a deadline, not technical debt.

## Fallback
If the live deploy is unreachable, `docs/fallback.mp4` is a recording of the exact path above, captured in `DEMO_MODE`.
